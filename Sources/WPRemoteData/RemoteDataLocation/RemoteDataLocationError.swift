//
//  RemoteDataLocationError.swift
//  RemoteData
//
//  Created by William Piotrowski on 11/18/20.
//  Copyright © 2020 William Piotrowski. All rights reserved.
//
/*
import SPCommon

public enum RemoteDataLocationError: ScorepioError {
    case missingQuerySnapshot
    case couldNotInitializeData
    case missingSequenceDataTypeString
    case sequenceDataTypeStringDoesNotMatch(string: String)
    
    public var message: String {
        switch self {
        case .missingQuerySnapshot: return "No query snapshot returned and no error from the server."
        case .couldNotInitializeData: return "Could not initialize one or more of the returned objects."
        case .missingSequenceDataTypeString: return "Could not determine FirestoreDoc.Type because sequenceDataType string is missing"
        case .sequenceDataTypeStringDoesNotMatch(let string): return "Could not determine FirestoreDoc.Type because no known match for string: \(string)."
        }
    }
}
*/
